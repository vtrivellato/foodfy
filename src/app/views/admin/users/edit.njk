{% extends "../layout.njk" %}

{% block content %}

    <form class="details-card" method="post" action="/admin/users/{{ user.id }}?_method=put">
        <div class="flex-row" style="justify-content: space-between;">
            <h3>Alterando usuário</h3>
            
            <div>
                <button class="button" type="submit">Salvar</button>
            </div>
        </div>

        {% include './fields.njk' %}
        <div class="form-side flex-row">
            <div class="form-field-area" style="width: 50%;">
                <h4>Senha</h4>

                <input id="password "type="password" name="password" >
            </div>

            <div class="form-field-area" style="width: 50%;">
                
            </div>
        </div>
    </form>

{% endblock %}